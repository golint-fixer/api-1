machine:
  environment:
    GOPATH: "$GOPATH:$HOME"

dependencies:
  override:
    - go get github.com/Masterminds/glide
    - glide install
    - go install .

test:
  pre:
    - go vet

  override:
    - go test
