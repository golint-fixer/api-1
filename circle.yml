machine:
  environment:
    GOPATH: "$GOPATH:$HOME"

dependencies:
  override:
    - go get github.com/Masterminds/glide
    - glide install
    - go install github.com/thedodd/buildAPI

test:
  pre:
    - go vet

  override:
    - go test
